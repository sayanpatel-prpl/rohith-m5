---
phase: 08-forward-looking-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/watchlist/Watchlist.tsx
  - src/sections/watchlist/WatchlistSummaryStats.tsx
  - src/sections/watchlist/FundraiseSignals.tsx
  - src/sections/watchlist/MarginInflection.tsx
  - src/sections/watchlist/ConsolidationTargets.tsx
  - src/sections/watchlist/StressIndicators.tsx
autonomous: true

must_haves:
  truths:
    - "User sees fundraise signals with company, signal description, confidence badge, timeframe, and expandable details"
    - "User sees margin inflection candidates with current vs projected margin, catalyst, and confidence"
    - "User sees consolidation targets with rationale, likely acquirers, and confidence"
    - "User sees stress indicators with severity badge (critical/warning/watch) and expandable details"
    - "Summary stats show total counts per signal category"
    - "Radix Tabs allow filtering between signal categories"
  artifacts:
    - path: "src/sections/watchlist/Watchlist.tsx"
      provides: "Main Watchlist section with Radix Tabs and summary stats"
      min_lines: 40
    - path: "src/sections/watchlist/WatchlistSummaryStats.tsx"
      provides: "4-column StatCard grid for signal counts"
      min_lines: 15
    - path: "src/sections/watchlist/FundraiseSignals.tsx"
      provides: "Fundraise signal cards with confidence and timeframe"
      min_lines: 25
    - path: "src/sections/watchlist/MarginInflection.tsx"
      provides: "Margin inflection cards with current vs projected margin"
      min_lines: 25
    - path: "src/sections/watchlist/ConsolidationTargets.tsx"
      provides: "Consolidation target cards with acquirer list"
      min_lines: 25
    - path: "src/sections/watchlist/StressIndicators.tsx"
      provides: "Stress indicator cards with severity badges"
      min_lines: 25
  key_links:
    - from: "src/sections/watchlist/Watchlist.tsx"
      to: "src/hooks/use-filtered-data.ts"
      via: "useFilteredData('watchlist')"
      pattern: "useFilteredData.*watchlist"
    - from: "src/sections/watchlist/FundraiseSignals.tsx"
      to: "src/components/ui/ConfidenceBadge.tsx"
      via: "ConfidenceBadge for signal confidence"
      pattern: "ConfidenceBadge"
---

<objective>
Build the Watchlist & Forward Indicators section -- 90-day predictive intelligence showing likely fundraises, margin inflection candidates, consolidation targets, and stress indicators.

Purpose: Users need forward-looking signals to plan BD outreach timing. This section answers "what's likely to happen next?" for companies in the coverage universe.

Output: Fully rendered Watchlist section replacing the Phase 2 placeholder, with tabbed navigation across 4 signal categories.
</objective>

<execution_context>
@/Users/prateekkurkanji/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prateekkurkanji/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/types/sections.ts
@src/data/mock/watchlist.ts
@src/hooks/use-filtered-data.ts
@src/components/ui/StatCard.tsx
@src/components/ui/ConfidenceBadge.tsx
@src/components/ui/TrendIndicator.tsx
@src/components/ui/PerformanceTag.tsx
@src/lib/formatters.ts
@src/theme/tokens.css

Reference patterns:
@src/sections/deals/DealsTransactions.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Watchlist section with 4 signal categories in tabbed layout</name>
  <files>src/sections/watchlist/Watchlist.tsx, src/sections/watchlist/WatchlistSummaryStats.tsx, src/sections/watchlist/FundraiseSignals.tsx, src/sections/watchlist/MarginInflection.tsx, src/sections/watchlist/ConsolidationTargets.tsx, src/sections/watchlist/StressIndicators.tsx</files>
  <action>
Replace the Phase 2 placeholder in `Watchlist.tsx` with a full Watchlist section. Create sub-components for each signal category. The main component must retain `export default function Watchlist()` (React.lazy requires default export).

**Watchlist.tsx** (main orchestrator):
- Keep `useFilteredData<WatchlistData>("watchlist")`, loading/error/null handling, header with `DataRecencyTag`.
- Remove placeholder content and `getActiveFilterSummary` helper.
- Layout:
  ```
  <div className="p-md space-y-md">
    {/* Header with "Watchlist & Forward Indicators" title + DataRecencyTag */}
    <WatchlistSummaryStats data={data} />
    {/* Radix Tabs */}
    <Tabs.Root defaultValue="all">
      <Tabs.List className="flex gap-xs border-b border-surface-overlay mb-md">
        <TabTrigger value="all" label="All Signals" />
        <TabTrigger value="fundraise" label="Fundraise" count={data.fundraiseSignals.length} />
        <TabTrigger value="inflection" label="Margin Inflection" count={data.marginInflectionCandidates.length} />
        <TabTrigger value="consolidation" label="Consolidation" count={data.consolidationTargets.length} />
        <TabTrigger value="stress" label="Stress" count={data.stressIndicators.length} />
      </Tabs.List>
      <Tabs.Content value="all">
        <div className="space-y-md">
          <FundraiseSignals signals={data.fundraiseSignals} />
          <MarginInflection candidates={data.marginInflectionCandidates} />
          <ConsolidationTargets targets={data.consolidationTargets} />
          <StressIndicators indicators={data.stressIndicators} />
        </div>
      </Tabs.Content>
      <Tabs.Content value="fundraise"><FundraiseSignals signals={data.fundraiseSignals} /></Tabs.Content>
      <Tabs.Content value="inflection"><MarginInflection candidates={data.marginInflectionCandidates} /></Tabs.Content>
      <Tabs.Content value="consolidation"><ConsolidationTargets targets={data.consolidationTargets} /></Tabs.Content>
      <Tabs.Content value="stress"><StressIndicators indicators={data.stressIndicators} /></Tabs.Content>
    </Tabs.Root>
  </div>
  ```
- Import: `import { Tabs } from "radix-ui"`
- TabTrigger helper following the same pattern as DealsTransactions and CompetitiveMoves.

**WatchlistSummaryStats.tsx**:
- Props: `{ data: WatchlistData }`
- 4 StatCards in `grid grid-cols-4 gap-md`:
  - Fundraise Signals (count)
  - Margin Inflection (count)
  - Consolidation Targets (count)
  - Stress Indicators (count)

**FundraiseSignals.tsx**:
- Props: `{ signals: WatchlistData['fundraiseSignals'] }`
- Section heading: "Likely Fundraises (90-day)" in `text-xs font-medium text-text-secondary uppercase tracking-wide`
- Cards in `space-y-sm`:
  - Company name in bold + ConfidenceBadge with `level={signal.confidence}`
  - Signal description in `text-xs text-text-primary`
  - Timeframe: "Expected within {signal.timeframeMonths} months" in `text-xs text-text-muted`
  - Expandable details via Radix Collapsible: button "Details" toggles details text
  - Use `import { Collapsible } from "radix-ui"`

**MarginInflection.tsx**:
- Props: `{ candidates: WatchlistData['marginInflectionCandidates'] }`
- Section heading: "Margin Inflection Candidates"
- Cards in `space-y-sm`:
  - Company name + ConfidenceBadge
  - Current vs projected margin displayed with arrow: `{currentMarginPct}% → {projectedMarginPct}%`
  - Format margins with 1 decimal place. Use green text for projected if higher than current.
  - Margin improvement: `+{(projected - current).toFixed(1)}% improvement` in `text-xs text-positive font-medium`
  - Catalyst text in `text-xs text-text-secondary`

**ConsolidationTargets.tsx**:
- Props: `{ targets: WatchlistData['consolidationTargets'] }`
- Section heading: "Consolidation Targets"
- Cards in `space-y-sm`:
  - Company name + ConfidenceBadge
  - Rationale in `text-xs text-text-primary`
  - Likely acquirers as pills: `flex flex-wrap gap-1`, each as `text-xs bg-surface-overlay px-1.5 py-0.5 rounded text-text-muted`

**StressIndicators.tsx**:
- Props: `{ indicators: WatchlistData['stressIndicators'] }`
- Section heading: "Stress Indicators"
- Cards in `space-y-sm`:
  - Company name + severity badge (config-record: critical = `bg-negative/10 text-negative border-negative/20`, warning = `bg-brand-accent/10 text-brand-accent border-brand-accent/20`, watch = `bg-neutral/10 text-neutral border-neutral/20`)
  - Indicator text in `text-xs text-text-primary`
  - Expandable details via Radix Collapsible
  - Add left border coloring by severity: critical = `border-l-2 border-l-negative`, warning = `border-l-2 border-l-brand-accent`, watch = `border-l-2 border-l-neutral`

**Important constraints:**
- Watchlist uses display company names (not IDs) -- this data is AI-generated forward signals, not filterable by company
- Import Radix: `import { Tabs } from "radix-ui"` and `import { Collapsible } from "radix-ui"`
- Reuse ConfidenceBadge for all confidence displays
- Follow the tab trigger pattern from DealsTransactions
  </action>
  <verify>
Run `npx tsc --noEmit` -- zero TypeScript errors. Verify:
1. Summary stats show 4 StatCards with counts.
2. Tabs switch between All, Fundraise, Margin Inflection, Consolidation, Stress.
3. Fundraise signals show with confidence badges and expandable details.
4. Margin inflection shows current vs projected margins with improvement.
5. Consolidation targets show with likely acquirer pills.
6. Stress indicators show with severity badges and left border coloring.
  </verify>
  <done>Watchlist section displays 4 forward-looking signal categories with tabbed navigation, confidence badges, and expandable details. Requirements WTCH-01 through WTCH-04 satisfied.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Fundraise signals with confidence and timeframe (WTCH-01)
- Margin inflection candidates with current vs projected (WTCH-02)
- Consolidation targets with rationale and likely acquirers (WTCH-03)
- Stress indicators with severity badges (WTCH-04)
- Tabbed navigation across all 4 signal categories
</verification>

<success_criteria>
Watchlist section renders 4 forward-looking signal categories with tabbed navigation, summary stats, confidence badges, and expandable details. Requirements WTCH-01 through WTCH-04 are complete.
</success_criteria>

<output>
After completion, create `.planning/phases/08-forward-looking-signals/08-01-SUMMARY.md`
</output>
